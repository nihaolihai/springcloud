server:
  port: 9001

spring:
  application:
    name: cloud-nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:1111 #设置nacos地址

management:
  endpoints:
  web:
    exposure:
      include: '*'