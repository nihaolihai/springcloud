server:
  port: 9002

spring:
  application:
    name: cloud-nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #设置nacos地址

management:
  endpoints:
  web:
    exposure:
      include: '*'